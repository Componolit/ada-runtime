with "aunit";

project test is

   for Source_Dirs use (".", "../../src/lib/");
   for Main use ("test.adb");

   package Binder is
      for Switches ("Ada") use ("-static");
   end Binder;

end test;
